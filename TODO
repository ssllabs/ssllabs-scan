
TODO
----

- Send the original JSON response to output, not the Go version.

- Make the testing resumable.

- Read hostnames from a file.

- Output assessment results in SSL Pulse CSV format, which is designed for
  import into a relational database.
